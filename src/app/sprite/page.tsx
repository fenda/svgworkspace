import { ToolPlaceholder } from "@/components/layout/ToolPlaceholder";

export default function SpritePage() {
  return (
    <ToolPlaceholder
      title="Sprite"
      description="Build and manage SVG sprites without uploading files to a server."
    />
  );
}
