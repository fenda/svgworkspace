import { ToolPlaceholder } from "@/components/layout/ToolPlaceholder";

export default function InspectPage() {
  return (
    <ToolPlaceholder
      title="Inspect"
      description="Inspect SVG structure, metadata, and accessibility details locally."
    />
  );
}
