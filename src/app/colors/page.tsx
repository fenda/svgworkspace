import { ToolPlaceholder } from "@/components/layout/ToolPlaceholder";

export default function ColorsPage() {
  return (
    <ToolPlaceholder
      title="Colors"
      description="Replace hardcoded fills and manage SVG color tokens in the browser."
    />
  );
}
