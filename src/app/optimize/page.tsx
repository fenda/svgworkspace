import { ToolPlaceholder } from "@/components/layout/ToolPlaceholder";

export default function OptimizePage() {
  return (
    <ToolPlaceholder
      title="Optimize"
      description="SVG optimization tools will run entirely in your browser."
    />
  );
}
