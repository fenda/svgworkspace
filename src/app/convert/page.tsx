import { ToolPlaceholder } from "@/components/layout/ToolPlaceholder";

export default function ConvertPage() {
  return (
    <ToolPlaceholder
      title="Convert"
      description="Convert SVGs to React, Vue, HTML, and more — client-side only."
    />
  );
}
